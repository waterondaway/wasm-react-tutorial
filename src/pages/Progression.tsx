import { Box, Typography } from "@mui/material";

function Progression() {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4" sx={{ mb: 3 }}>
                Progression
            </Typography>
            <Typography variant="body1">
                This is the Progression page.
            </Typography>
        </Box>
    );
}

export default Progression;
