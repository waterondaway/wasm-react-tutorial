import { Box, Typography } from "@mui/material";

function RetuneRetrain() {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4" sx={{ mb: 3 }}>
                Retune & Retrain
            </Typography>
            <Typography variant="body1">
                This is the Retune & Retrain page.
            </Typography>
        </Box>
    );
}

export default RetuneRetrain;
